# Copie este arquivo para .env e preencha os valores reais.
# NUNCA faça commit do .env — apenas do .env.example.

SUPABASE_URL=https://jvqlxrixzqlbwmmdwcob.supabase.co
SUPABASE_SERVICE_KEY=eyJ...  # chave service_role (Supabase → Project Settings → API)
